{
  "id": "gbrain",
  "kind": "context-engine",
  "name": "gbrain",
  "version": "0.0.0",
  "description": "Test-time fixture for openclaw-loads-the-plugin e2e. Mirrors the real plugin id contract and keeps the loader surface minimal.",
  "configSchema": {},
  "contracts": {
    "contextEngines": [
      "gbrain-context"
    ]
  }
}
